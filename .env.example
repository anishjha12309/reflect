# Reflect environment — copy to .env (git-ignored) and fill in.
# All providers use no-card free tiers (CLAUDE.md §2, §4).

# --- LLM inference providers ---
CEREBRAS_API_KEY=
GROQ_API_KEY=
GEMINI_API_KEY=
OPENROUTER_API_KEY=

# --- Search / retrieval ---
TAVILY_API_KEY=
SERPER_API_KEY=
# Local SearXNG (docker compose) — final fallback, no quota.
SEARXNG_URL=http://localhost:8080
