# docs/

Drop the 30-second demo recording here as `demo.gif` — the root `README.md` embeds it at
the top (`![Reflect demo](docs/demo.gif)`).

Suggested capture: enter a topic → show the plan appearing → the live activity log →
the report streaming in with `[n]` citations → the provider/quota strip. Keep it ≤ ~5 MB so
it renders inline on GitHub.
